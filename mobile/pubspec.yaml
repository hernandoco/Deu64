name: deu64_mobile
description: App de escenas + quiz (carrusel) consumiendo API en Go.
publish_to: 'none'

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.2

flutter:
  uses-material-design: true
  assets:
    - assets/images/placeholder.png